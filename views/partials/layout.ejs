<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Library Management System</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
        }
        .container {
            max-width: <%= maxWidth || '800px' %>;
            margin: 0 auto;
            background-color: white;
            padding: 40px;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        .back-link {
            display: inline-block;
            margin-bottom: 20px;
            color: #007bff;
            text-decoration: none;
            padding: 8px 16px;
            border: 1px solid #007bff;
            border-radius: 4px;
            transition: all 0.3s;
        }
        .back-link:hover {
            background-color: #007bff;
            color: white;
        }
        <% if (additionalStyles) { %>
        <%- additionalStyles %>
        <% } %>
    </style>
</head>
<body>
    <div class="container">
        <% if (showBackLink) { %>
        <a href="<%= backUrl || '/' %>" class="back-link">← <%= backText || 'Back to Main Menu' %></a>
        <% } %>
        <%- body %>
    </div>
    <% if (additionalScripts) { %>
    <%- additionalScripts %>
    <% } %>
</body>
</html>