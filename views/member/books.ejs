<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            margin: 0;
            padding: 0;
            min-height: 100vh;
        }

        .member-header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 2px solid #e0e0e0;
            padding: 20px 0;
            margin-bottom: 30px;
        }

        .header-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo-section {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .logo-section h1 {
            color: #2c3e50;
            margin: 0;
            font-size: 1.8em;
        }

        .back-link {
            color: #3498db;
            text-decoration: none;
            font-weight: 500;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        .member-nav {
            display: flex;
            gap: 15px;
        }

        .nav-link {
            padding: 8px 16px;
            background: #3498db;
            color: white;
            text-decoration: none;
            border-radius: 6px;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .nav-link:hover {
            background: #2980b9;
            transform: translateY(-1px);
        }

        .nav-link.logout {
            background: #e74c3c;
        }

        .nav-link.logout:hover {
            background: #c0392b;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .page-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .page-header h2 {
            color: white;
            font-size: 2.2em;
            margin: 0 0 10px 0;
        }

        .page-header p {
            color: rgba(255, 255, 255, 0.9);
            font-size: 1.1em;
            margin: 0;
        }

        .search-section {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        }

        .search-form {
            display: flex;
            gap: 15px;
            align-items: center;
            flex-wrap: wrap;
        }

        .search-input {
            flex: 1;
            min-width: 250px;
            padding: 12px 16px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 16px;
            transition: border-color 0.3s ease;
        }

        .search-input:focus {
            outline: none;
            border-color: #3498db;
        }

        .search-btn {
            padding: 12px 24px;
            background: #3498db;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .search-btn:hover {
            background: #2980b9;
            transform: translateY(-1px);
        }

        .clear-btn {
            background: #95a5a6;
        }

        .clear-btn:hover {
            background: #7f8c8d;
        }

        .results-info {
            color: rgba(255, 255, 255, 0.9);
            text-align: center;
            margin-bottom: 30px;
            font-size: 1.1em;
        }

        .books-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 25px;
            margin-bottom: 40px;
        }

        .book-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 25px;
            text-align: center;
            transition: all 0.3s ease;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        }

        .book-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 35px rgba(0, 0, 0, 0.2);
        }

        .book-icon {
            font-size: 3em;
            margin-bottom: 15px;
            color: #3498db;
        }

        .book-title {
            font-size: 1.3em;
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 10px;
            line-height: 1.3;
        }

        .book-author {
            color: #7f8c8d;
            font-size: 1em;
            margin-bottom: 20px;
        }

        .book-details {
            font-size: 0.9em;
            color: #95a5a6;
            margin-bottom: 20px;
        }

        .availability-indicator {
            display: inline-flex;
            align-items: center;
            gap: 5px;
            font-size: 0.9em;
            font-weight: 600;
            margin-bottom: 15px;
            padding: 6px 12px;
            border-radius: 20px;
            text-align: center;
        }

        .availability-available {
            background: rgba(46, 204, 113, 0.15);
            color: #27ae60;
            border: 1px solid rgba(46, 204, 113, 0.3);
        }

        .availability-unavailable {
            background: rgba(231, 76, 60, 0.15);
            color: #e74c3c;
            border: 1px solid rgba(231, 76, 60, 0.3);
        }

        .availability-limited {
            background: rgba(241, 196, 15, 0.15);
            color: #f39c12;
            border: 1px solid rgba(241, 196, 15, 0.3);
        }

        .book-actions {
            display: flex;
            gap: 10px;
            justify-content: center;
        }

        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 6px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 5px;
        }

        .btn-primary {
            background: #3498db;
            color: white;
        }

        .btn-primary:hover {
            background: #2980b9;
            transform: translateY(-1px);
        }

        .btn-secondary {
            background: #95a5a6;
            color: white;
        }

        .btn-secondary:hover {
            background: #7f8c8d;
            transform: translateY(-1px);
        }

        .pagination {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            margin-top: 40px;
        }

        .pagination a,
        .pagination span {
            padding: 10px 15px;
            background: rgba(255, 255, 255, 0.95);
            color: #2c3e50;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .pagination a:hover {
            background: #3498db;
            color: white;
            transform: translateY(-1px);
        }

        .pagination .current {
            background: #3498db;
            color: white;
        }

        .pagination .disabled {
            background: rgba(255, 255, 255, 0.5);
            color: #bdc3c7;
            cursor: not-allowed;
        }

        .no-books {
            text-align: center;
            padding: 60px 20px;
            color: rgba(255, 255, 255, 0.9);
        }

        .no-books h3 {
            font-size: 1.8em;
            margin-bottom: 15px;
        }

        .no-books p {
            font-size: 1.1em;
            margin-bottom: 20px;
        }

        .alert {
            padding: 15px 20px;
            border-radius: 8px;
            margin-bottom: 25px;
            font-weight: 500;
            text-align: center;
        }

        .alert-success {
            background-color: rgba(212, 237, 218, 0.9);
            color: #155724;
            border: 1px solid rgba(195, 230, 203, 0.9);
        }

        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                gap: 15px;
            }

            .search-form {
                flex-direction: column;
            }

            .search-input {
                min-width: 100%;
            }

            .books-grid {
                grid-template-columns: 1fr;
            }

            .pagination {
                flex-wrap: wrap;
            }
        }
    </style>
</head>

<body>
    <div class="member-header">
        <div class="header-content">
            <div class="logo-section">
                <span style="font-size: 2em;">📚</span>
                <h1>Library Portal</h1>
                <span>•</span>
                <a href="/member/dashboard" class="back-link">← Dashboard</a>
            </div>
            <div class="member-nav">
                <a href="/member/dashboard" class="nav-link">Dashboard</a>
                <a href="/member/borrowed" class="nav-link">My Books</a>
                <a href="/profile" class="nav-link">Profile</a>
                <a href="/logout" class="nav-link logout">Logout</a>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="page-header">
            <h2>📖 Browse Books</h2>
            <p>Discover your next great read from our collection</p>
        </div>

        <% if (message) { %>
            <div class="alert alert-success">
                <%= message %>
            </div>
            <% } %>

                <div class="search-section">
                    <form method="GET" action="/member/books" class="search-form">
                        <input type="text" name="search" value="<%= search || '' %>"
                            placeholder="Search by title, author, or keyword..." class="search-input">
                        <button type="submit" class="search-btn">🔍 Search</button>
                        <% if (search) { %>
                            <a href="/member/books" class="search-btn clear-btn">Clear</a>
                            <% } %>
                    </form>
                </div>

                <div class="results-info">
                    <% if (search) { %>
                        <p>📊 Found <strong>
                                <%= totalBooks %>
                            </strong> books matching "<%= search %>" • Available books shown first</p>
                        <% } else { %>
                            <p>📊 Showing <strong>
                                    <%= books.length %>
                                </strong> of <strong>
                                    <%= totalBooks %>
                                </strong> books • Available books shown first</p>
                            <% } %>
                </div>

                <% if (books && books.length> 0) { %>
                    <div class="books-grid">
                        <% books.forEach(book=> { %>
                            <div class="book-card">
                                <div class="book-icon">📚</div>
                                <div class="book-title">
                                    <%= book.Title %>
                                </div>
                                <div class="book-author">by <%= book.Author %>
                                </div>

                                <!-- Availability Indicator -->
                                <% if (book.availableCopies !== undefined) { %>
                                    <% if (book.availableCopies > 0) { %>
                                        <% if (book.availableCopies >= 3) { %>
                                            <div class="availability-indicator availability-available">
                                                ✅ Available (<%= book.availableCopies %> copies)
                                            </div>
                                        <% } else { %>
                                            <div class="availability-indicator availability-limited">
                                                ⚠️ Limited availability (<%= book.availableCopies %> <%= book.availableCopies === 1 ? 'copy' : 'copies' %>)
                                            </div>
                                        <% } %>
                                    <% } else { %>
                                        <div class="availability-indicator availability-unavailable">
                                            ❌ Currently unavailable
                                        </div>
                                    <% } %>
                                <% } %>

                                <% if (book.Genre || book.PublicationYear) { %>
                                    <div class="book-details">
                                        <% if (book.Genre) { %>
                                            <span>Genre: <%= book.Genre %></span><br>
                                            <% } %>
                                                <% if (book.PublicationYear) { %>
                                                    <span>Published: <%= book.PublicationYear %></span>
                                                    <% } %>
                                    </div>
                                    <% } %>

                                        <div class="book-actions">
                                            <a href="/member/books/<%= book.ID %>" class="btn btn-primary">
                                                👁️ View Details
                                            </a>
                                        </div>
                            </div>
                            <% }); %>
                    </div>

                    <% if (pagination.totalPages> 1) { %>
                        <div class="pagination">
                            <% if (pagination.hasPrev) { %>
                                <a
                                    href="?page=<%= pagination.prevPage %><%= search ? '&search=' + encodeURIComponent(search) : '' %>">←
                                    Previous</a>
                                <% } else { %>
                                    <span class="disabled">← Previous</span>
                                    <% } %>

                                        <span class="current">Page <%= pagination.currentPage %> of <%=
                                                    pagination.totalPages %></span>

                                        <% if (pagination.hasNext) { %>
                                            <a
                                                href="?page=<%= pagination.nextPage %><%= search ? '&search=' + encodeURIComponent(search) : '' %>">Next
                                                →</a>
                                            <% } else { %>
                                                <span class="disabled">Next →</span>
                                                <% } %>
                        </div>
                        <% } %>
                            <% } else { %>
                                <div class="no-books">
                                    <h3>📭 No Books Found</h3>
                                    <% if (search) { %>
                                        <p>We couldn't find any books matching "<%= search %>".</p>
                                        <a href="/member/books" class="btn btn-primary">Browse All Books</a>
                                        <% } else { %>
                                            <p>No books are currently available in the library.</p>
                                            <% } %>
                                </div>
                                <% } %>
    </div>
</body>

</html>